#include "strategy.hpp"

std::vector<int> generate_signals(const std::vector<double>& prices, int short_window, int long_window) {
    // TODO: Implement real logic
    return std::vector<int>(prices.size(), 0);  // Dummy: all zeros
}
